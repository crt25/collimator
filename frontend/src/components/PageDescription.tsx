import { chakra } from "@chakra-ui/react";

const PageDescription = chakra("p", {
  base: {
    fontSize: "2xl",
    color: "textSecondary",
    marginTop: "md",
  },
});

export default PageDescription;
