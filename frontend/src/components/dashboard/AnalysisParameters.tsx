import styled from "@emotion/styled";

const AnalysisParameters = styled.div`
  padding: 1rem;
  border: var(--foreground-color) 1px solid;
  border-radius: var(--border-radius);

  margin-bottom: 1rem;

  select,
  input {
    width: 100%;
  }
`;

export default AnalysisParameters;
